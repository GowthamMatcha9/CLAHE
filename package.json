{
  "name": "histogram-equalizations",
  "version": "1.0.0",
  "private": true,
  "description": "CLAHE Image Calculator packaged as an Android app with Capacitor",
  "scripts": {
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "build:android": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "8.4.2",
    "@capacitor/android": "8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2"
  }
}
